import { dev } from "$app/environment";

export const title: string = 'ML-with-Julia';
export const description: string = 'Machine Learning with Julia';
export const url: string = dev ? 'http://localhost:3000' : 'https://ml-with-julia.vercel.app';
export const version: string = '0.1.0';
